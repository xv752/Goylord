# Goylord Client Environment Configuration
# Copy this file to .env or set as environment variables

# Server URL - MUST use wss:// (TLS is always enabled on server)
GOYLORD_SERVER=wss://your-server.com:5173

# TLS Configuration
# For development with self-signed certificates:
# GOYLORD_TLS_INSECURE_SKIP_VERIFY=true

# For production, trust the server certificate (copy server.crt from server):
# GOYLORD_TLS_CA=./certs/server.crt
GOYLORD_TLS_INSECURE_SKIP_VERIFY=false

# Client certificate for mutual TLS (optional, advanced)
# GOYLORD_TLS_CLIENT_CERT=./certs/client.crt
# GOYLORD_TLS_CLIENT_KEY=./certs/client.key

# Mutex (single-instance) configuration
# Leave empty or set to "none" to allow multiple instances
# GOYLORD_MUTEX=goylord-client

# Capture Configuration
GOYLORD_CAPTURE_INTERVAL=20s
GOYLORD_DISABLE_CAPTURE=false

# Mode: dev, development, local, release, prod, production
GOYLORD_MODE=production
